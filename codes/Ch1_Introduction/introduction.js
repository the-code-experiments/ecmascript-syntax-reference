/**
 * Understanding `var` & `let` behavior in terms of declaration.
 *
 */
require('./eg1_let');

/**
 * More understanding `var` & `let` behavior in terms of redeclaration.
 */
require('./eg2_let');

/**
 * More understanding `var` & `let` behavior in terms of for loop.
 */
require('./eg3_let');
